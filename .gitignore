# Reconstruction safety nets — kept on disk, but not in git history
_before-recovery/
_my-rebuild-backup/

# Local-only configs (user pastes their own keys)
supabase-config.local.js

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Spike test artifacts (keep on disk for local dev, not in git history)
node_modules/
output.jpg
test-room.jpg

# Supabase CLI cache — auto-generated, machine-specific
supabase/.temp/
supabase/.branches/
